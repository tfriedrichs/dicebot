/**
 * Package containing all classes which handle the computation of a single result from a dice roll.
 * <p>
 * This is necessary because each {@link io.github.tfriedrichs.dicebot.result.DiceRoll}
 * could potentially be valued in many different ways and a dice expression needs to evaluable to a single value.
 */
package io.github.tfriedrichs.dicebot.evaluator;